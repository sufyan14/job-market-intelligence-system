## Database Design Rationale

The database schema is normalized to support:
- Skill-level analysis
- Job clustering
- Salary prediction
- API scalability

Separate tables are used for skills, clusters, and predictions
to avoid redundancy and support efficient querying.

